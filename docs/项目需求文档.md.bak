# 车载 Qi 无线充电模块 — 项目需求文档

> **文档编号**: QI-PRD-001  
> **版本**: V1.7  
> **日期**: 2026-08-17  
> **状态**: 更新 — 标注实际实现状态  
> **CAN MCU**: AT32F426KBU7-4 (Artery, Cortex-M4F, 180MHz, 128KB Flash, 20KB SRAM)

---

## 一、项目概述

### 1.1 项目背景

本项目开发一款车载 Qi 无线充电模块，集成于车辆中控区域，通过 Qi 无线充电协议为兼容设备提供最高 15W 的无线充电服务。系统采用 **双 MCU 架构**：CAN MCU (AT32F426) 负责 CAN 通信、诊断、CAN-UART 桥接中继、加密和 GPIO 控制；Qi 无线充芯片负责无线充电核心功能（充电协议、功率控制、FOD、热保护等）。模块通过 CAN 总线与车辆中央控制单元（CCU）通信，支持完整的 UDS 诊断及异物检测（FOD）功能。

**AT32 角色说明**：AT32F426 具备双重角色：

(1) **自身 OTA**：通过 CAN/UDS 协议支持 MCU 固件远程升级（双槽 A/B 布局 + 试启动回滚）；
(2) **CAN-UART 桥接中继**：作为 CCU 与 Qi 无线充芯片之间的通讯桥梁，数据流向: Host (CCU) → CAN → MCU (AT32) → UART → Qi 无线充芯片。MCU 不参与充电核心逻辑。

### 1.2 项目目标

| 目标 | 指标 |
|------|------|
| 充电功率 | 最高 15W (Qi v1.3 EPP) |
| 通信可靠性 | 24h 连续运行无丢帧 |
| 启动时间 | 上电到 OPERATIONAL < 2s |
| 工作温度 | -20°C ~ +60°C (环境) |

### 1.3 MCU 芯片规格

#### CAN MCU — AT32F426KBU7-4

| 参数 | 规格 |
|------|------|
| 内核 | ARM Cortex-M4F (带 FPU/DSP) |
| 主频 | 最高 180 MHz |
| Flash | 128 KB |
| SRAM | 20 KB |
| 封装 | QFN32 |
| 工作温度 | -40°C ~ +105°C |
| CAN-FD | 支持 (本项目使用 Classical CAN 2.0B) |
| 通信接口 | SPI, I2C, UART, CAN |
| 定时器 | 多路通用/高级定时器 |

#### Qi 无线充芯片

> 具体型号待确认 (OI-07)，负责无线充电核心功能。

| 参数 | 说明 |
|------|------|
| 无线充电协议 | Qi v1.3 EPP |
| 最大功率 | 15W |
| 通信接口 | UART (与 CAN MCU 通信) |
| 核心功能 | 充电状态机、FOD 检测、功率管理、热保护、ADC 采样 |


### 1.4 Flash 布局规划 (CAN MCU)

| 区域 | 地址范围 | 大小 | 说明 |
|------|----------|------|------|
| Bootloader | 0x0800_0000 ~ 0x0800_3FFF | 16 KB | 启动加载器 (OTA 逻辑、签名验证、槽位选择) |
| APP_A (槽位 0) | 0x0800_4000 ~ 0x0800_FFFF | 48 KB | 主应用固件 |
| APP_B (槽位 1) | 0x0801_0000 ~ 0x0801_BFFF | 48 KB | 备份固件 (OTA 双槽 A/B) |
| OTA 元数据 (主) | 0x0801_C000 ~ 0x0801_DFFF | 8 KB | 启动元数据、槽位状态、试启动信息 |
| OTA 元数据 (备) | 0x0801_E000 ~ 0x0801_FFFF | 8 KB | 元数据备份 (掉电安全) |

> **说明**: AT32F426 支持通过 CAN/UDS 协议的自身 OTA 升级。采用双槽 A/B 布局，活跃槽在下载期间不可擦除。启动元数据采用主备双份 + CRC32 校验保证原子性。详细实施方案见 [MCU-OTA详细实施方案.md](MCU-OTA详细实施方案.md)。
>
> **实际实现**：Flash 布局已在 Bootloader 和 APP 两个工程中实现，详见 [flash.md](flash.md)。
> **OTA 升级流程**：实际代码流程与需求存在差异（0x2E/0x31 步骤未实现），详见 6.1 节和 [workflow-detail.md](workflow-detail.md)。

---

## 二、系统架构

### 2.1 整体拓扑 (双 MCU 架构)

```
┌─────────────────────────────────────────────────────┐
│               客户端 CCU / 上位机 (调试)              │
│     行程管理器 / 云接口 / Qi 充电器 UDS 客户端        │
└───────────────┬─────────────────────────────────────┘
                │ CAN 250 kbps, 29-bit 扩展帧
                │ CANH, CANL, 12V, GND
┌───────────────┴─────────────────────────────────────┐
│          CAN MCU (AT32F426, 地址 0x0D, UDS 服务端)    │
│                                                      │
│  ┌──────────┐ ┌──────────────┐ ┌────────┐ ┌──────────┐  │
│  │ UDS 诊断 │ │ CAN-UART 桥接│ │ 加密   │ │ GPIO 控制│  │
│  │ 服务栈   │ │ 协议转换/中继│ │ 模块   │ │ 夹臂/LED │  │
│  └──────────┘ └──────────────┘ └────────┘ └──────────┘  │
│  ┌──────────┐ ┌──────────────┐ ┌──────────────────────┐  │
│  │ 遥测引擎 │ │ 串口通信     │ │ Bootloader           │  │
│  │ 广播/DID │ │ Qi芯片接口   │ │ 上电引导/OTA/签名验证 │  │
│  └──────────┘ └──────────────┘ └──────────────────────┘  │
│                                                      │
│  MCU: AT32F426KBU7-4    外设: NTC/ADC/Hall/GPIO/CAN  │
└──────────────────┬──────────────────────────────────┘
                   │ Serial UART (115200 bps)
                   │ 帧头 0x55 0xAA, CRC16-CCITT
┌──────────────────┴──────────────────────────────────┐
│            Qi 无线充芯片                              │
│                                                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────────┐  │
│  │ 充电状态 │ │ FOD/热   │ │ Qi TX 硬件接口       │  │
│  │ 机       │ │ 保护     │ │ 线圈/驱动IC/传感器    │  │
│  └──────────┘ └──────────┘ └──────────────────────┘  │
│  ┌──────────┐ ┌──────────┐                           │
│  │ 功率管理 │ │ ADC 采样 │                           │
│  │          │ │ 温度/电压│                           │
│  └──────────┘ └──────────┘                           │
│                                                      │
│  Qi 芯片: 型号待定    不处理 CAN/加密/OTA 逻辑       │
└──────────────────────────────────────────────────────┘
                │ Qi 磁感应
                ▼
          手机 / Qi 接收器
```

**上位机说明**：调试测试阶段，上位机通过 CAN 转接卡模拟客户端 CCU，发送 UDS 指令与 CAN MCU 通信。功能简易即可，量产环境由真实客户端 CCU 替代。

### 2.2 CAN MCU 功能边界

> 下表标注各功能的实际实现状态。

| 功能 | CAN MCU (AT32F426) | Qi 无线充芯片 |
|------|---------------------|---------------|
| CAN 通信 (UDS) | ✅ 已实现（收发）；⬜ ISO-TP 多帧待实现 | ❌ |
| 自身 OTA | ✅ 核心已实现（Bootloader + 下载 + Trial 框架）；⬜ ECDSA/Trigger 链路待完成 | ❌ |
| 串口通信 | 🔄 底层已实现（9600 8N1 + FIFO）；⬜ 协议解析层待实现 | ✅ 通过串口上报状态、接收控制指令 |
| 加密 | ⬜ ECDSA P-256 待实现（当前占位） | ❌ |
| GPIO 控制 | ⬜ 待实现 | ❌ |
| 充电状态机 | ❌ 不处理 | ✅ 11 状态管理 |
| FOD 检测 | ❌ 不处理 | ✅ 异物检测 |
| 功率管理 | ❌ 不处理 | ✅ 功率协商/降额 |
| 热保护 | ❌ 不处理 | ✅ 温度监控/降额/暂停 |
| ADC 采样 | ❌ 不处理 (由 Qi 芯片完成) | ✅ 温度/电压/电流 |
| Qi 协议栈 | ❌ 不处理 | ✅ Qi v1.3 EPP |

### 2.3 软件架构分层 (CAN MCU — 裸机超级循环)

```
┌─────────────────────────────────────────────────────┐
│  应用层 (Application Layer)                          │
│  状态机驱动：正常/服务/制造/运输模式切换               │
│  GPIO 控制器、故障管理器、配置管理器                   │
├─────────────────────────────────────────────────────┤
│  服务层 (Service Layer)                              │
│  UDS 诊断服务、CAN-UART 桥接管理、遥测引擎、加密模块 │
│  串口通信管理、NVM 持久化                             │
├─────────────────────────────────────────────────────┤
│  驱动层 (Driver Layer)                               │
│  CAN 驱动 (CAN 2.0B + ISO-TP)、UART 驱动            │
│  GPIO 驱动、NVM 驱动 (内部 Flash)、Timer/WDG 驱动    │
├─────────────────────────────────────────────────────┤
│  硬件层 (Hardware)                                   │
│  AT32F426KBU7-4 (Cortex-M4F, 180MHz)               │
└─────────────────────────────────────────────────────┘
```

**主循环 (Super Loop)** — 实际代码入口 `qi_wireless_code/mdk_user/Src/main.c`：
```c
int main(void) {
    SCB->VTOR = APP_BASE_ADDR;    // 设置向量表偏移 (0x08004000)
    system_clock_config();         // 180MHz
    nvic_priority_group_config(NVIC_PRIORITY_GROUP_4);
    timer_drv_init();              // 软件定时器 + SysTick 1ms
    wdg_drv_init();                // IWDG ~1s
    nvm_drv_init();                // NVM 配置驱动
    can_driver_init();             // CAN1 250kbps 扩展帧
    qi_uart_init();                // USART2 9600 8N1
    ota_confirm_if_needed();       // Trial 确认
    can_protocol_init();           // 注册 UDS 回调
    timer_create(100, broadcast_timer_callback, 1);  // 100ms 广播
    send_broadcast();              // 上电即发 BOOTUP 广播

    while (1) {
        timer_poll();              // 软件定时器轮询
        can_driver_poll();         // CAN 帧分发
        qi_uart_poll();            // Qi UART 数据轮询
        wdg_drv_refresh();         // 喂狗
        if (g_broadcast_flag) { g_broadcast_flag = 0; send_broadcast(); }
    }
}
```

> **与需求差异**：实际代码中未包含 GPIO 初始化、状态机、充电业务逻辑等功能（待实现）。
> 详细工作流参见 [workflow-detail.md](workflow-detail.md) 第 4 节。

**架构说明：**
- 裸机开发，不使用 RTOS，通过超级循环 + 状态机实现多任务调度
- 各模块通过轮询（polling）方式处理 I/O，关键中断（CAN Rx/Tx、UART Rx）在 ISR 中设置标志，主循环中处理
- 状态机管理运行模式切换（正常/服务/制造/运输），各模式下启用或禁用对应功能模块
- 时基由 SysTick 定时器提供，用于超时检测、去抖、周期广播等定时功能

| 层级 | 模块 | 说明 | 状态 |
|------|------|------|------|
| **Bootloader** | 启动选择 | 槽位选择、试启动、回滚控制 | ✅ 已实现 |
| | 签名验证 | ECDSA P-256 镜像验签 | ⬜ 占位（TODO） |
| | 元数据管理 | 主备双份元数据读写、CRC32 校验 | ✅ 已实现 |
| | OTA 下载引擎 | Bootloader 内精简 UDS/CAN 驱动 | ✅ 已实现（单帧） |
| **应用层** | 状态机 | 模式管理（正常/服务/制造/运输） | ⬜ 待实现 |
| | GPIO 控制器 | 夹臂电机、霍尔传感器、LED | ⬜ 待实现 |
| | 故障管理器 | CAN 通信故障、内部故障处理 | ⬜ 待实现 |
| | 配置管理器 | NVM 持久化、操作模式 | ✅ NVM 已实现 |
| | OTA 确认模块 | APP 侧试启动确认接口 | ✅ 已实现 |
| **服务层** | UDS 诊断服务 | DID 读写、会话管理、安全访问 | 🔄 基础服务已实现 |
| | CAN-UART 桥接管理 | 协议转换、数据中继、错误处理 | ⬜ 待实现 |
| | 遥测引擎 | 周期性广播、状态上报 | ✅ 广播已实现 |
| | 加密模块 | ECDSA P-256 签名验证 | ⬜ 待实现 |
| | 串口通信管理 | Qi 芯片数据收发与解析 | ⬜ 底层已通，协议解析待实现 |
| **驱动层** | CAN 驱动 | CAN 2.0B 收发、ISO-TP 分帧 | ✅ CAN 已实现，ISO-TP 待实现 |
| | UART 驱动 | 与 Qi 芯片串口通信 | ✅ 已实现 |
| | GPIO 驱动 | 霍尔传感器、LED、控制引脚 | ⬜ 待实现 |
| | NVM 驱动 | 内部 Flash 读写 | ✅ 已实现 |
| | Timer/WDG 驱动 | 定时器、看门狗 | ✅ 已实现 |
| **硬件层** | AT32F426KBU7-4 | Cortex-M4F, 180MHz, 128KB Flash, 20KB SRAM | — |

### 2.4 硬件接口清单 (CAN MCU)

| 接口 | 类型 | 连接对象 | 信号特征 |
|------|------|----------|----------|
| CAN 收发器 | CAN | TJA1050/MCP2562 | 250kbps, 2.0B 扩展帧 |
| Qi 芯片串口 | UART | Qi 无线充芯片 | 9600 8N1, 0x55 0xAA 帧头 |
| 霍尔传感器 | GPIO | 夹臂霍尔开关 (左右) | 数字输入，低有效，50ms 去抖 |
| 夹臂电机 | GPIO | H桥驱动电路 | PWM/方向控制 |
| NVM | 内部 Flash | AT32F426 内部 Flash | 按需读写 |
| 状态 LED | GPIO | LED 驱动电路 | 推挽输出 |
| 看门狗 | IWDG | 内部 | 1000ms 超时 |

### 2.5 CAN MCU 引脚资源分配 (AT32F426KBU7-4)

> 详细引脚定义见 [AT32F426KBU7-4_引脚定义.md](AT32F426KBU7-4_引脚定义.md)

**芯片**: AT32F426KBU7-4 | **封装**: QFN32 (32 + EPAD) | **来源**: MCU引脚定义说明20260729.xlsx

#### 引脚资源汇总

| 功能分类 | 引脚数 | 引脚列表 | 说明 |
|----------|--------|----------|------|
| 电源 (VDD/VDDA/GND) | 3 | Pin1, Pin5, EPAD | 3.3V 数字/模拟电源 + 接地 |
| 时钟 (HEXT) | 2 | Pin2 (PF0), Pin3 (PF1) | 外部 8MHz 晶振 |
| 复位/启动 | 2 | Pin4 (NRST), Pin31 (BOOT0) | 复位 + 启动选择 |
| CAN 通信 | 2 | Pin21 (PA11/CAN_RX), Pin22 (PA12/CAN_TX) | 内置 CAN 控制器 |
| SPI (CAN FD 收发器) | 4 | Pin10 (PA4/CS), Pin11 (PA5/SCK), Pin12 (PA6/MISO), Pin13 (PA7/MOSI) | SIT1145AQT SPI 接口 |
| UART1 (CAN MCU↔Qi 芯片) | 2 | Pin29 (PB6/TX), Pin30 (PB7/RX) | 115200bps 串口 |
| UART2 (Qi 芯片通信) | 2 | Pin8 (PA2/TX), Pin9 (PA3/RX) | 9600 8N1 串口 |
| 传感器/控制 | 3 | Pin6 (PA0/HALL), Pin15 (PB1/12V_EN), Pin16 (PB2/5V_EN) | 霍尔检测 + 电源使能 |
| 调试接口 (SWD) | 2 | Pin23 (PA13/SWDIO), Pin24 (PA14/SWCLK) | SWD 烧录 |
| 未使用 (NC) | 10 | PA1, PA8~PA10, PA15, PB0, PB4, PB5, PB8, PB10 | 输出低电平 |
| 特殊 | 1 | Pin26 (PB3) | CAN FD 收发器悬空，输出低电平 |
| **合计** | **32 + EPAD** | | |

#### 关键引脚复用说明

| 引脚 | 默认功能 | 可选复用 | 注意事项 |
|------|----------|----------|----------|
| PA0 (Pin6) | HALL 霍尔检测 | ADC1_IN0, WKUP1 | 兼做唤醒源 |
| PA11/PA12 (Pin21/22) | CAN_RX/TX | — | CAN 控制器固定引脚 |
| PA4~PA7 (Pin10~13) | SPI1_CS/SCK/MISO/MOSI | I2S1, TMR3/TMR4 | CAN FD 收发器专用 |
| PB6/PB7 (Pin29/30) | USART1_TX/RX | I2C1_SCL/SDA | 与 Qi 芯片通信 |
| PA2/PA3 (Pin8/9) | USART2_TX/RX | — | Qi 芯片串口 |

---

## 三、CAN 通信协议

### 3.1 物理层参数

| 参数 | 规格 |
|------|------|
| 标准 | ISO 11898-1 CAN 2.0B |
| 波特率 | 250 kbps ± 0.5% |
| 帧格式 | 扩展帧 (29-bit ID) |
| DLC | 8 字节 (固定) |
| 字节序 | Big-Endian (MSB First) |
| 采样点 | 80% (推荐) |
| 总线负载 | ≤ 50% (诊断时 ≤ 70%) |
| CAN 控制器 | AT32F426 内置 CAN 控制器 |

### 3.2 CAN ID 分配

| 通道 | CAN ID | 用途 |
|------|--------|------|
| UDS 请求 (CCU→Qi) | 0x18DA0D03 | 诊断请求 |
| UDS 响应 (Qi→CCU) | 0x18DA030D | 诊断响应 |
| 功能请求 | 0x18DB33D0 | UDS 功能寻址广播 |
| 生命周期广播 | 0x18FF260D | Qi 模块状态上报 (100ms) |
| CCU 控制 | 0x18FF270D | CCU 控制指令 |

### 3.3 ISO-TP 传输层

| 参数 | 值 |
|------|-----|
| Block Size (BS) | 0 (无限制) |
| STmin | 0 ms |
| FC.CTS 超时 | 1000 ms |
| CF 间隔超时 | 1000 ms |
| 单帧最大数据 | 7 字节 |
| 多帧最大数据 | 4095 字节 |

### 3.4 UDS 服务集

> 下表标注各服务的实际实现状态（基于 `workflow-detail.md` 和源码）。

| SID | 服务 | 会话 | 安全 | 状态 | 说明 |
|-----|------|------|------|------|------|
| 0x10 | DiagnosticSessionControl | 全部 | 无 | ✅ 已实现 | Bootloader Safe Mode + APP 端 |
| 0x11 | ECUReset | Ext/Prog | Level 1 | ✅ 已实现 | Bootloader: 等待 IWDG 复位；APP: 触发 OTA |
| 0x22 | ReadDataByIdentifier | Extended | 无 | ⬜ 待实现 | 未实现 |
| 0x27 | SecurityAccess | Ext/Prog | — | ⚠️ 简化版 | 当前接受任意 seed/key，未实现 ECDSA P-256 |
| 0x2E | WriteDataByIdentifier | Extended | Level 1 | ⬜ 待实现 | 未实现 |
| 0x31 | RoutineControl | Ext/Prog | 视例程 | ⬜ 待实现 | 未实现 |
| 0x34 | RequestDownload | Programming | Level 1 | ✅ 已实现 | 擦除 APP_A + 初始化下载状态 |
| 0x36 | TransferData | Programming | Level 1 | ✅ 已实现 | Flash word 编程 + 块序号校验 |
| 0x37 | RequestTransferExit | Programming | Level 1 | ✅ 已实现 | CRC32 计算 + Image Header 写入 + metadata 更新 |
| 0x3E | TesterPresent | 全部 | 无 | ✅ 已实现 | APP 端回显保活 |

**与需求差异说明**：
- `0x27 SecurityAccess` — 需求要求 ECDSA P-256 验签，当前为简化版（直接放行）。**详见 `workflow-detail.md` 第 9 节**
- `0x2E WriteDataByIdentifier` — 需求中用于选择固件类型（0x2010），实际代码中未实现。OTA 下载直接由 0x34 触发
- `0x31 RoutineControl` — 需求中用于擦除目标槽位（0xFF00），实际代码中擦除在 0x34 处理中直接执行
- ISO-TP 多帧 — 未实现，当前仅支持单帧 UDS（每帧 8 字节 CAN 数据，0x36 载荷 ≤ 6 字节）
- 会话管理 — Bootloader Safe Mode 无会话概念（直接处理）；APP 端仅支持 Default Session

### 3.5 安全访问 (ECDSA P-256)

> ⚠️ **当前实现状态**：Bootloader Safe Mode 下的 SecurityAccess（0x27）为简化版，**不做实际校验，接受任意 seed/key**。ECDSA P-256 签名验证为占位（TODO）。详见 [workflow-detail.md](workflow-detail.md) 第 3.4 节和第 9 节。

根据 2026-07-23 更新，安全访问已从 Ed25519 迁移为 **ECDSA P-256** (FIPS 186-4 / NIST secp256r1)：

- 挑战 seed 先用 SHA-256 哈希，再进行签名
- 签名为原始 R‖S，固定 64 字节 (IEEE P1363 格式)
- 公钥为 65 字节非压缩 SEC1 点 (0x04 ‖ X ‖ Y)
- 私钥为 32 字节标量
- 失败锁定：连续 3 次失败 → 60s 锁定
- 加密逻辑由 CAN MCU 实现

### 3.6 生命周期广播 (0x18FF260D, 100ms 周期)

| Byte | 字段 | 长度 | 说明 |
|------|------|------|------|
| 0 | 充电状态 | 8-bit | 0x00~0x0A (从 Qi 芯片串口获取) |
| 1 | 故障码 | 8-bit | 最高优先级故障 |
| 2 [7:4] | FOD 状态 | 4-bit | 正常/可疑/确认 (从 Qi 芯片获取) |
| 2 [3:0] | 操作模式 | 4-bit | 正常/服务/制造/运输 |
| 3 | 线圈温度 | 8-bit | 有符号, 1°C/LSB, 偏移+40 |
| 4 | PCB 温度 | 8-bit | 有符号, 1°C/LSB, 偏移+40 |
| 5 | 输入电压 | 8-bit | 0.1V/LSB |
| 6 [7:4] | 功率高4位 | 4-bit | — |
| 7 | 功率低8位 | 8-bit | 0.1W/LSB |

> **数据来源**：充电状态、FOD 状态、温度、电压、功率等数据由 Qi 芯片通过串口上报，CAN MCU 组包广播。

---

## 四、CAN MCU ↔ Qi 芯片串口通信协议

> 本协议基于《数据通信协议表-IAP.xlsx》，CAN MCU 与 Qi 芯片之间通过 UART 串口通信。Qi 芯片自行处理充电功能，通过串口向 CAN MCU 上报状态数据并接收控制指令。

### 4.1 物理层

| 参数 | 值 |
|------|-----|
| 波特率 | 9600 bps |
| 数据位 | 8 |
| 停止位 | 1 |
| 校验 | 无 |
| 流控 | 无 |

> **注意**：实际代码实现（`qi_uart.c`）使用 9600 bps，与本表一致。部分需求文档中提到的 115200 bps 为早期版本，已更正。

### 4.2 帧格式

| 字段 | 长度 | 说明 |
|------|------|------|
| 帧起始头 | 2 字节 | 0x55 0xAA |
| 帧长度 | 1 字节 | 仅包含数据长度：帧命令 + 帧数据 + 帧流水号 |
| 帧命令 | 1 字节 | 见 4.3 命令码定义 |
| 帧数据 | n 字节 | 具体数据（根据命令码不同而不同） |
| 帧流水号 | 1 字节 | 1~255，循环递增 |
| 帧校验 | 1 字节 | (帧起始头 + 帧长度 + 帧命令 + 帧数据 + 帧流水号) 取低八位 |

### 4.3 命令码定义

| 命令码 | 方向 | 说明 |
|--------|------|------|
| 0x00 | Qi→MCU | 回复指令（ACK/NAK） |
| 0x01 | Qi→MCU | 无线充上报数据（状态 + 功率 + 版本） |
| 0x02 | MCU→Qi | 设置功率 |
| 0xCC | MCU→Qi | IAP 流程（进入 IAP / 发送数据） |

### 4.4 回复指令 (0x00)

Qi 芯片对 MCU 命令的应答。

**帧格式：**

| 偏移 | 长度 | 字段 | 值/说明 |
|------|------|------|--------|
| 0-1 | 2 | 帧起始头 | `0x55 0xAA` |
| 2 | 1 | 帧长度 | `0x03` (固定) |
| 3 | 1 | 帧命令 | `0x00` |
| 4 | 1 | 应答状态 | `0x00` = 接收成功 |
| 5 | 1 | 帧流水号 | 与请求帧一致 |
| 6 | 1 | 帧校验 | 低 8 位累加和 |

**Qi 芯片应答示例**: `55 AA 03 00 00 SEQ CS`

### 4.5 无线充上报数据 (0x01)

Qi 芯片周期性向 CAN MCU 上报当前充电状态和芯片信息。帧长度固定 `0x08`。

**帧格式：**

| 偏移 | 长度 | 字段 | 值/说明 |
|------|------|------|--------|
| 0-1 | 2 | 帧起始头 | `0x55 0xAA` |
| 2 | 1 | 帧长度 | `0x08` (固定) |
| 3 | 1 | 帧命令 | `0x01` |
| 4 | 1 | 状态字节1 | 见下方位定义 |
| 5 | 1 | 状态字节2 | 见下方位定义 |
| 6-7 | 2 | 实时功率 | 高字节在前，单位待确认 |
| 8-9 | 2 | 版本号 | Qi 芯片固件版本，高字节在前 |
| 10 | 1 | 帧流水号 | 1~255 |
| 11 | 1 | 帧校验 | 低 8 位累加和 |

**状态字节1 位定义：**

| Bit | 名称 | 说明 |
|-----|------|------|
| b0 | PING 标识 | 1 = 检测到设备放置 |
| b1 | 充电状态 | 1 = 正在充电 |
| b2 | 满充状态 | 1 = 电池已充满 |
| b3 | 过压保护 | 1 = 触发过压保护 |
| b4 | 欠压保护 | 1 = 触发欠压保护 |
| b5 | 过流保护 | 1 = 触发过流保护 |
| b6 | 过温保护 | 1 = 触发过温保护 |
| b7 | FOD 保护 | 1 = 触发异物检测保护 |

**状态字节2 位定义：**

| Bit | 名称 | 说明 |
|-----|------|------|
| b0 | 充电次数 | 低 8 位 |
| b1-b7 | 保留 | 保留 |

**CAN MCU 处理逻辑：**
- 解析状态字节1 各保护标志位，生成内部故障事件
- 解析实时功率用于 DID 0x2104 (输出功率) 上报
- 解析版本号用于设备信息管理
- 周期性上报数据用于生命周期广播 (0x18FF260D)

### 4.6 设置功率 (0x02)

CAN MCU 向 Qi 芯片设置目标充电功率。帧长度固定 `0x03`。

**帧格式：**

| 偏移 | 长度 | 字段 | 值/说明 |
|------|------|------|--------|
| 0-1 | 2 | 帧起始头 | `0x55 0xAA` |
| 2 | 1 | 帧长度 | `0x03` (固定) |
| 3 | 1 | 帧命令 | `0x02` |
| 4 | 1 | 功率值 | `0x01`=5W, `0x02`=10W, `0x03`=15W |
| 5 | 1 | 帧流水号 | 1~255 |
| 6 | 1 | 帧校验 | 低 8 位累加和 |

Qi 芯片收到后应回复 0x00 (ACK) 或 NAK。

### 4.7 IAP 协议 (0xCC)

CAN MCU 通过串口对 Qi 芯片进行固件升级（IAP）。IAP 地址空间为 16-bit (0x0000 ~ 0xFFFF)，每帧最大数据量 22 字节。

#### 4.7.1 子命令汇总

| 子命令 | 方向 | 说明 |
|--------|------|------|
| 0x01 | MCU→Qi | 准备升级（含固件大小） |
| 0x01 应答 | Qi→MCU | 应答确认 |
| 0x02 | MCU→Qi | 发送固件数据（含地址 + 数据） |
| 0x02 应答 | Qi→MCU | 应答确认 |

#### 4.7.2 Step 1: 准备升级 (0xCC, 子命令 0x01)

MCU 通知 Qi 芯片进入 IAP 模式，帧长度 `0x04`。

**帧格式：**

| 偏移 | 长度 | 字段 | 值/说明 |
|------|------|------|--------|
| 0-1 | 2 | 帧起始头 | `0x55 0xAA` |
| 2 | 1 | 帧长度 | `0x04` |
| 3 | 1 | 帧命令 | `0xCC` |
| 4 | 1 | IAP 子命令 | `0x01` (准备升级) |
| 5 | 1 | APP 大小高字节 | 固件大小 Bit[15:8] |
| 6 | 1 | APP 大小低字节 | 固件大小 Bit[7:0] |
| 7 | 1 | 帧校验 | 低 8 位累加和 |

**示例帧**: `55 AA 04 CC 01 32 B9 BB`

**Qi 芯片应答**: `55 AA 04 CC 01 00 00 CS`

| 偏移 | 字段 | 说明 |
|------|------|------|
| 4 | IAP 子命令 | `0x01` (回应准备升级) |
| 5 | 状态 | `0x00` = 成功, 其他 = 失败 |
| 6 | 保留 | `0x00` |

#### 4.7.3 Step 2: 发送固件数据 (0xCC, 子命令 0x02)

MCU 向 Qi 芯片发送 APP 固件数据，每帧最多 22 字节。

**帧格式：**

| 偏移 | 长度 | 字段 | 值/说明 |
|------|------|------|--------|
| 0-1 | 2 | 帧起始头 | `0x55 0xAA` |
| 2 | 1 | 帧长度 | 可变 (1+2+数据长度+1) |
| 3 | 1 | 帧命令 | `0xCC` |
| 4 | 1 | IAP 子命令 | `0x02` (发送数据) |
| 5 | 1 | 当前地址高字节 | Flash 写入地址 Bit[15:8] |
| 6 | 1 | 当前地址低字节 | Flash 写入地址 Bit[7:0] |
| 7~N | 最多22 | APP 数据 | 固件数据 |
| N+1 | 1 | 帧校验 | 低 8 位累加和 |

**示例帧 (第1包, 22字节数据)**: `55 AA 1A CC 02 00 00 [22字节数据] CS`

帧长度 `0x1A` = 26 = 1(IAP子命令) + 2(地址) + 22(数据) + 1(帧流水号)

**Qi 芯片应答**: `55 AA 04 CC 02 00 00 CS`

| 偏移 | 字段 | 说明 |
|------|------|------|
| 4 | IAP 子命令 | `0x02` (回应数据接收) |
| 5 | 状态 | `0x00` = 成功 |
| 6 | 保留 | `0x00` |

#### 4.7.4 IAP 升级流程

```
准备升级 (0xCC/0x01) → 收到应答 OK →
发送数据 (0xCC/0x02) × N 次 → 每次收到应答 OK →
全部数据发送完成
```

**CAN MCU IAP 管理要求：**
- 升级前确认 Qi 芯片当前不在充电状态
- 每包数据等待应答后再发送下一包
- 传输失败时终止升级并上报故障

---

## 五、故障检测与保护

### 5.1 CAN MCU 侧故障

| 故障码 | 名称 | 优先级 | 阻塞充电 | 说明 |
|--------|------|--------|----------|------|
| 0x0A | CAN 通信故障 | 4 | 否 | CAN 总线异常 / bus-off |
| 0x0B | 内部硬件故障 | 1 | 是 | MCU 内部错误 |
| 0x0C | 串口通信超时 | 2 | 是 | 与 Qi 芯片通信中断 |

### 5.2 Qi 芯片侧故障 (通过串口上报)

| 故障码 | 名称 | 优先级 | 阻塞充电 | 说明 |
|--------|------|--------|----------|------|
| 0x01 | 过温 (线圈) | 1 (最高) | 是 | Qi 芯片检测并上报 |
| 0x02 | 过温 (PCB) | 1 | 是 | Qi 芯片检测并上报 |
| 0x03 | FOD 确认 | 2 | 是 | Qi 芯片检测并上报 |
| 0x04 | 输入过压 | 2 | 是 | Qi 芯片检测并上报 |
| 0x05 | 输入欠压 | 3 | 是 | Qi 芯片检测并上报 |
| 0x06 | 输入过流 | 2 | 是 | Qi 芯片检测并上报 |
| 0x07 | Qi RX 通信超时 | 3 | 是 | Qi 芯片检测并上报 |
| 0x08 | 传感器失效 (温度) | 2 | 是 | Qi 芯片检测并上报 |
| 0x09 | 传感器失效 (电压/电流) | 3 | 是 | Qi 芯片检测并上报 |

### 5.3 CAN MCU 故障处理流程

1. Qi 芯片通过串口上报故障信息
2. CAN MCU 解析故障码，按优先级仲裁
3. CAN MCU 生成故障 DID (0x210B)，响应 UDS 读取
4. CAN MCU 在生命周期广播中携带最高优先级故障码
5. CCU 根据故障码决定是否禁用充电

---

## 六、OTA 远程升级

### 6.1 MCU 自身 OTA 升级

CAN MCU (AT32F426) 支持通过 CAN/UDS 协议的自身固件远程升级，采用**双槽 A/B 布局 + 试启动回滚**机制。

> **实际实现流程**（基于 `workflow-detail.md`，与源码一致）：

**升级流程**:

```
主机/CCU                          APP (0x08004000+)                Bootloader (0x08000000)
    │                                  │                                │
    │ ① 0x34 / 0x11 (触发 OTA)        │                                │
    ├─────────────────────────────────▶│                                │
    │                                  │ 写 metadata                    │
    │                                  │ (ota_state=DOWNLOADING)        │
    │                                  │ NVIC_SystemReset()             │
    │ ② 0x74 正响应                     │                                │
    │◀─────────────────────────────────┤                                │
    │                                  │─────── 系统复位 ─────────────▶│
    │                                  │                                │ ③ 检测 ota_state==DOWNLOADING
    │                                  │                                │   → 进入 Safe Mode
    │ ④ 0x34 RequestDownload (在 Safe Mode 下)                           │
    ├─────────────────────────────────────────────────────────────────▶│ ⑤ 擦除 APP_A 24 扇区
    │◀─────────────────────────────────────────────────────────────────┤ ⑥ 0x74,0x20,0xC0,0x00
    │ ⑦ 0x36 TransferData (seq=1..N) │                                │
    ├─────────────────────────────────────────────────────────────────▶│ ⑧ word 编程
    │◀─────────────────────────────────────────────────────────────────┤ 0x76,seq
    │ ⑨ 0x37 RequestTransferExit      │                                │ ⑩ CRC32 + Image Header
    ├─────────────────────────────────────────────────────────────────▶│  更新 metadata
    │◀─────────────────────────────────────────────────────────────────┤ ⑪ 0x77
    │ ⑫ 0x11 ECUReset                 │                                │
    ├─────────────────────────────────────────────────────────────────▶│ ⑬ 0x51，等待 IWDG
    │                                  │                                │
    │                                  │──── 看门狗复位(~1s) ─────────▶│ ⑭ 重新引导：校验 APP_A
    │                                  │                                │  → 通过 → 跳转至 APP_A+256
    │                                  │◀──── 跳转 ─────────────────────┤
    │                                  │ ⑮ 上电广播 0x18FF260D          │
    │                                  │ ⑯ 若 Trial ACTIVE → CONFIRMED  │
```

**与需求差异说明**：
- 需求中的 `0x2E 选择固件类型` 和 `0x31 擦除目标槽位` 在实际代码中未实现
- 实际代码中擦除操作在 0x34 RequestDownload 处理中直接执行（擦除 APP_A 全部 24 个 2KB 扇区）
- 安全访问（0x27）当前为简化版，未实现 ECDSA P-256
- 详细工作流参见 [workflow-detail.md](workflow-detail.md)
- Bootloader 详细流程参见 [bootloader-flow.md](bootloader-flow.md)
- Flash 布局参见 [flash.md](flash.md)

**关键设计**:
- Flash 四区布局：Bootloader (16KB) + APP_A (48KB) + APP_B (48KB) + OTA 元数据 (16KB)
- 启动元数据主备双份 + CRC32，保证掉电安全
- 试启动 3 次重试上限（retry_count > max_retries 触发回滚）
- ECDSA P-256 签名验证 — **占位（TODO）**，当前仅 CRC32 校验
- 公钥存储在 Bootloader 只读区，量产时启用 Flash 写保护

> 详细实施方案见 [MCU-OTA详细实施方案.md](MCU-OTA详细实施方案.md)

### 6.2 Qi 芯片 OTA (CAN-UART 实时中继升级)

本项目 Qi 芯片 OTA 链路: **CCU → CAN → MCU (实时中继) → UART → Qi 芯片**。

MCU 作为 CAN 与 UART 之间的**实时中继桥梁**，不缓存 Qi 固件（128KB Flash 已被 MCU 自身 OTA 双槽布局占满）:
1. CCU 通过 CAN (UDS 0x34/0x36/0x37) 逐帧下发 Qi 固件数据
2. MCU 收到每个 TransferData 帧后，**实时**打包成 UART IAP 帧 (0xCC) 转发给 Qi 芯片
3. MCU 不存储 Qi 固件，仅做协议转换

**Qi 芯片 OTA 升级流程 (实时中继):**

```
CCU (Host)                MCU (AT32F426)              Qi 芯片
    │                          │                          │
    │── CAN: 0x10 0x02 ──────→│                          │
    │   (进入编程会话)          │                          │
    │                          │                          │
    │── CAN: 0x27 (ECDSA) ──→│                          │
    │   (安全认证)              │                          │
    │                          │                          │
    │── CAN: 0x2E 0x2010 ───→│                          │
    │   (选择固件类型=Qi 0x02)  │                          │
    │                          │                          │
    │── CAN: 0x34 (下载请求) ─→│                          │
    │                          │                          │
    │── CAN: 0x36 (数据[0]) ─→│── UART: 0xCC 0x01 ─────→│
    │                          │   (IAP准备, 含固件大小)    │
    │                          │←── UART: ACK ────────────│
    │                          │                          │
    │── CAN: 0x36 (数据[1]) ─→│── UART: 0xCC 0x02 ─────→│
    │                          │   (数据: addr + 22字节)   │
    │                          │←── UART: ACK ────────────│
    │                          │                          │
    │        ... 逐帧转发 ...   │                          │
    │                          │                          │
    │── CAN: 0x37 (传输结束) ─→│                          │
    │── CAN: 0x11 (复位) ────→│── 复位 Qi 芯片 ─────────→│
```

**CAN MCU 实时中继要求:**
- MCU 收到每个 CAN TransferData 帧后立即通过 UART 转发，不缓存整个固件
- Qi 芯片 UART IAP 每帧最多传输 22 字节数据
- 升级前确认 Qi 芯片当前不在充电状态
- 每包 UART 数据等待应答后再发送下一包
- UART 传输失败时通过 CAN NRC 向 CCU 报错，终止升级
- Qi 芯片 IAP 完成后，MCU 等待 Qi 芯片重新上线并验证版本号
- 具体 UART IAP 帧格式见 4.7 节

**两个 OTA 通道对比:**

| 对比项 | MCU 自身 OTA (6.1) | Qi 芯片 OTA (6.2) |
|--------|---------------------|--------------------|
| 升级目标 | AT32F426 固件 | Qi 无线充芯片固件 |
| 传输链路 | CCU → CAN → MCU | CCU → CAN → MCU → UART → Qi |
| MCU 角色 | OTA 目标节点 | 实时中继桥梁 |
| 存储需求 | 双槽 A/B (96KB) | 无（实时转发） |
| 签名算法 | ECDSA P-256 | 由 Qi 芯片自行验证（如有） |
| 回滚机制 | 双槽试启动 + 自动回滚 | Qi 芯片自身机制 |
| 互斥性 | 两个 OTA 不能同时进行，进入 Programming Session 后停止充电和广播 |

---

## 七、配置与持久化

### 7.1 DID 清单 (CAN MCU 专用)

| DID | 名称 | 读 | 写 | 持久化 | 长度 | 数据来源 |
|-----|------|-----|-----|--------|------|----------|
| 0x2100 | 充电能力 | Ext | — | — | 4B | CAN MCU 本地 |
| 0x2101 | 充电使能 | Ext | Ext+L1 | 易失性 | 1B | CAN MCU 本地 |
| 0x2102 | 充电状态 | Ext | — | — | 1B | Qi 芯片串口上报 |
| 0x2103 | 设备存在 | Ext | — | — | 1B | Qi 芯片串口上报 |
| 0x2104 | 输出功率 | Ext | — | — | 2B | Qi 芯片串口上报 |
| 0x2105 | 输入电压 | Ext | — | — | 2B | Qi 芯片串口上报 |
| 0x2106 | 输入电流 | Ext | — | — | 2B | Qi 芯片串口上报 |
| 0x2107 | 线圈温度 | Ext | — | — | 2B | Qi 芯片串口上报 |
| 0x2108 | PCB 温度 | Ext | — | — | 2B | Qi 芯片串口上报 |
| 0x2109 | FOD 状态 | Ext | — | — | 1B | Qi 芯片串口上报 |
| 0x210A | RX 设备信息 | Ext | — | — | 8B | Qi 芯片串口上报 |
| 0x210B | 当前故障码 | Ext | — | — | 1B | 综合 (Qi + CAN MCU) |
| 0x210C | 热降额级别 | Ext | — | — | 1B | Qi 芯片串口上报 |
| 0x210D | 最大功率限制 | Ext | Ext+L1 | NVM | 2B | CAN MCU 本地 |
| 0x210E | 心跳间隔 | Ext | Ext+L1 | NVM | 2B | CAN MCU 本地 |
| 0x210F | 操作模式 | Ext | Ext+L1 | NVM | 1B | CAN MCU 本地 |
| 0x2110 | 最近故障详情 | Ext | — | — | 4B | 综合 |
| 0x2111 | 已传输能量 | Ext | — | — | 4B | Qi 芯片串口上报 |
| 0x2112 | OTA 状态 | Default | — | — | 1B | CAN MCU Bootloader/APP |
| 0x2113 | 活动固件槽位 | Default | — | — | 1B | CAN MCU Bootloader/APP |
| 0x2114 | 待定固件槽位 | Default | — | — | 1B | CAN MCU Bootloader/APP |
| 0x2115 | 最近启动原因 | Default | — | — | 1B | CAN MCU Bootloader |
| 0x2116 | 回滚计数器 | Default | — | — | 1B | CAN MCU Bootloader/APP |
| 0x2117 | 空闲超时 | Ext | Ext+L1 | NVM | 2B | CAN MCU 本地 |
| 0x2118 | 夹臂状态 | Default | — | — | 1B | CAN MCU GPIO |

### 7.2 操作模式

| 模式 | 值 | 充电 | 诊断 | 广播 | 低功耗 |
|------|-----|------|------|------|--------|
| 正常 | 0x00 | 使能 | 使能 | 使能 | 使能 |
| 服务 | 0x01 | 禁用 | 使能 | 使能 | 禁用 |
| 制造测试 | 0x02 | 禁用 | 使能 | 禁用 | 禁用 |
| 运输/存储 | 0x03 | 禁用 | 禁用 | 禁用 | 使能 |

---

## 八、非功能需求

### 8.1 性能时序

| 指标 | 要求 |
|------|------|
| UDS P2 响应 | ≤ 50 ms（安全访问验签支持 NRC 0x78 延迟响应，P2* 超时 5000ms） |
| 充电使能/禁用响应 | < 200 ms |
| 状态广播周期 | 100 ms ± 10 ms |
| CAN MCU ↔ Qi 芯片串口响应 | < 100 ms |
| FOD 关断时间 | < 500 ms (Qi 芯片侧) |
| 热保护关断时间 | < 500 ms (Qi 芯片侧) |
| MCU 启动时间 | < 2 s |
| CAN 唤醒恢复 | ≤ 100 ms |

### 8.2 可靠性

| 指标 | 要求 |
|------|------|
| CAN 稳定性 | 24h 连续无丢帧 |
| 上电循环 | 1000 次冷启动无故障 |
| Flash 写入寿命 | ≥ 100,000 次 (配置区) |
| MTBF | ≥ 50,000 小时 |

### 8.3 硬件约束 (CAN MCU)

| 参数 | 最小 | 典型 | 最大 | 单位 |
|------|------|------|------|------|
| 工作电压 | 10.0 | 12.0 | 15.0 | V |
| 瞬态电压 | — | — | 18.0 | V (< 100ms) |
| 待机电流 | — | 50 | 100 | mA |
| 环境温度 | -20 | 25 | 60 | °C |

### 8.4 看门狗

| 参数 | 值 |
|------|-----|
| 类型 | IWDG (独立看门狗) |
| 超时 | 500~2000 ms (推荐 1000ms) |
| 喂狗频率 | ≤ 500 ms |
| 复位后行为 | 充电禁用，进入 DISABLED |

---

## 九、供应商交付物

| 编号 | 交付物 | 格式 |
|------|--------|------|
| D-01 | 软件需求规格书 | DOCX/PDF |
| D-02 | 软件架构设计文档 | DOCX/PDF |
| D-03 | 详细设计文档 | DOCX/PDF |
| D-04 | CAN 通信数据库 | DBC |
| D-05 | UDS 诊断规范 | ODX/CDD |
| D-06 | 源代码 (含 Bootloader) | C/C++ |
| D-07 | 编译工具链配置 + 链接脚本 | Makefile/CMake + LD |
| D-08 | 单元测试报告 (67 项) | HTML/PDF |
| D-09 | 集成测试报告 (22 项) | HTML/PDF |
| D-10 | CAN 测试日志 (16 场景) | ASC/BLF |
| D-11 | 代码静态分析报告 | HTML/PDF |
| D-12 | 代码覆盖率报告 (≥80%) | HTML/PDF |
| D-13 | OTA 升级包生成工具 (MCU + Qi) | Python/CLI |
| D-14 | 量产烧录指南 (含公钥注入 + Flash 写保护) | PDF |
| D-15 | 版本发布说明 | PDF |
| D-16 | 接口控制文档 (CAN MCU ↔ Qi 芯片串口) | DOCX/PDF |
| D-17 | FMEA 分析报告 | XLSX/PDF |
| D-18 | 代码审查记录 | PDF |
| D-19 | 培训材料 | PPT/PDF |

---

## 十、待定事项

| 编号 | 事项 | 影响范围 | 优先级 | 状态 |
|------|------|----------|--------|------|
| OI-01 | MCU OTA 双槽 A/B 布局已确认：Bootloader(16KB) + APP_A(48KB) + APP_B(48KB) + 元数据(16KB) | OTA 架构 | P0 | ✅ 已确认并实现 |
| OI-02 | ECDSA P-256 签名验证实现（当前为占位 TODO） | 安全访问 | P0 | 🔴 待开发 |
| OI-03 | Qi 芯片型号确认 | 硬件接口 | P0 | ⬜ 待确认 |
| OI-04 | ~~CAN MCU ↔ Qi 芯片串口通信协议详细定义~~ → 已基于《数据通信协议表-IAP.xlsx》完成定义，见第四章 | 串口通信 | P0 | ✅ 已关闭 |
| OI-05 | ECDSA 公钥注入方式 (OTP/产线/预烧录) | 安全 | P0 | ⬜ 待确认 |
| OI-06 | Trial Boot PENDING 置位触发链路接通（0x37 处理中需补充） | OTA | P0 | 🔴 待修复 |
| OI-07 | CCU 控制指令完整定义 | CAN 通信 | P1 | ⬜ 待定义 |
| OI-08 | 运输模式唤醒/退出机制 | 低功耗 | P1 | ⬜ 待实现 |
| OI-09 | 看门狗/重试/去抖参数推荐值 | 可靠性 | P1 | ⬜ 待确认 |
| OI-10 | AT32F426 硬件加密引擎确认（有则用硬件加速，无则纯软件 micro-ecc） | 安全访问性能 | P0 | ⬜ 待确认 |
| OI-11 | APP 链接地址与 VTOR 对齐（scatter 配置待修改） | OTA 启动 | P0 | 🔴 待修复 |
| OI-12 | 0x37 未写回 active_slot（下载后需切换活跃槽位） | OTA 双槽 | P0 | 🔴 待修复 |
| OI-13 | ISO-TP 多帧支持（当前仅单帧 UDS） | 通信效率 | P1 | ⬜ 待实现 |

---

## 十一、参考文档

| 编号 | 文档 | 版本 |
|------|------|------|
| REF-01 | Qi Wireless Power Transfer Specification | v1.3.3 |
| REF-02 | ISO 14229-1 Unified Diagnostic Services | 2020 |
| REF-03 | ISO 15765-2 CAN Transport Protocol | 2016 |
| REF-04 | ISO 11898-1 CAN Data Link Layer | 2015 |
| REF-05 | AT32F426 Datasheet (DS_AT32F422_426_V2.01) | V2.01 |
| REF-06 | 通用 CAN 协议规范 (common_can_protocol_spec) | v1.0 |
| REF-07 | Qi 充电器 SRS (qi_charger_srs) | v1.1 |
| REF-08 | Qi SRS 更新 (2026-07-23) | — |
| REF-09 | [workflow-detail.md](workflow-detail.md) — 详细工作流文档（基于实际源码） | v1.0 |
| REF-10 | [bootloader-flow.md](bootloader-flow.md) — Bootloader 细节流程文档 | v1.0 |
| REF-11 | [flash.md](flash.md) — Flash 空间分配文档 | v1.0 |
| REF-12 | [测试用例表.md](测试用例表.md) — 测试用例文档 | v1.0 |
| REF-13 | [数据通信协议-IAP.md](数据通信协议-IAP.md) — MCU ↔ Qi 芯片 UART 通信协议 | v1.0 |
| REF-14 | [MCU-OTA详细实施方案.md](MCU-OTA详细实施方案.md) — MCU OTA 详细实施方案 | v1.0 |
| REF-15 | [OTA升级方案讨论总结.md](OTA升级方案讨论总结.md) — OTA 升级方案讨论总结 | v1.0 |
| REF-16 | [进度安排.md](进度安排.md) — 项目进度安排 | V2.0 |


---

## 十二、相关文件路径

> 以下路径供 OpenClaw 检索使用，文件均基于 WSL2 环境。项目根目录: `/home/nights/embedded_item/OTA-Upgrade-of-QI-Charger-Based-on-CAN/`

### 12.1 本项目文档

| 文件 | WSL 路径 |
|------|----------|
| 项目需求文档 | `/home/nights/embedded_item/OTA-Upgrade-of-QI-Charger-Based-on-CAN/docs/项目需求文档.md` |
| 数据通信协议-IAP | `/home/nights/embedded_item/OTA-Upgrade-of-QI-Charger-Based-on-CAN/docs/数据通信协议-IAP.md` |
| 进度安排 | `/home/nights/embedded_item/OTA-Upgrade-of-QI-Charger-Based-on-CAN/docs/进度安排.md` |
| AT32F426KBU7-4 引脚定义 | `/home/nights/embedded_item/OTA-Upgrade-of-QI-Charger-Based-on-CAN/docs/AT32F426KBU7-4_引脚定义.md` |
| 无线充软件 OTA 步骤 | `/home/nights/embedded_item/OTA-Upgrade-of-QI-Charger-Based-on-CAN/docs/OTA资料/无线充软件OTA步骤.docx` |
| MCU-OTA 详细实施方案 | `/home/nights/embedded_item/OTA-Upgrade-of-QI-Charger-Based-on-CAN/docs/MCU-OTA详细实施方案.md` |
| OTA 升级方案讨论总结 | `/home/nights/embedded_item/OTA-Upgrade-of-QI-Charger-Based-on-CAN/docs/OTA升级方案讨论总结.md` |

### 12.2 AT32 芯片资料

| 文件 | WSL 路径 |
|------|----------|
| AT32F422/426 寄存器手册 (Markdown) | `/home/whites/datasheet_floder/at32f422_426_register_datasheet/AT32F422_426_V2.00.md` |
| AT32F422/426 寄存器手册图片 | `/home/whites/datasheet_floder/at32f422_426_register_datasheet/images/` |
| AT32F422/426 规格书 (Markdown) | `/home/whites/datasheet_floder/at32f422_426_datasheet/` |
| AT32F422/426 数据手册 PDF (中文) | `/home/nights/embedded_item/OTA-Upgrade-of-QI-Charger-Based-on-CAN/docs/CAN软件资料/MCU资料/AT32F422_F426/DS_AT32F422_426_V2.00_ZH.pdf` |

### 12.3 协议与规范文档

| 文件 | WSL 路径 |
|------|----------|
| 通用 CAN 协议规范 (中文翻译) | `/home/nights/embedded_item/OTA-Upgrade-of-QI-Charger-Based-on-CAN/docs/CAN软件资料/SRS/common_can_protocol_spec_cn_翻译0517.docx` |
| Qi 充电器 SRS (中文翻译) | `/home/nights/embedded_item/OTA-Upgrade-of-QI-Charger-Based-on-CAN/docs/CAN软件资料/SRS/qi_charger_srs_cn_翻译0517.docx` |
| 数据通信协议表 (IAP) | `/home/nights/embedded_item/OTA-Upgrade-of-QI-Charger-Based-on-CAN/docs/CAN软件资料/数据通信协议表/数据通信协议表-IAP.xlsx` |

### 12.4 格式化技能参考

| 文件 | WSL 路径 |
|------|----------|
| Datasheet 格式化 SKILL.md | `/home/whites/.openclaw/workspace/skills/datasheet-formatting/SKILL.md` |

## 十三、变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| V1.0 | 2026-07-26 | 初稿，基于 docs/ 下全部文件整合 | Mr.Hu |
| V1.1 | 2026-08-10 | 新增第十一章「相关文件路径」，供 OpenClaw 检索使用 | Mr.Hu |
| V1.2 | 2026-08-10 | 新增2.5节「CAN MCU 引脚资源分配」，整合 AT32F426KBU7-4 引脚定义；新增文件路径至11.1 | Mr.Hu |
| V1.3 | 2026-08-14 | 基于《数据通信协议-IAP.md》修正第四章串口协议：补充帧格式表、状态位位定义；删除不存在的 IAP 子命令0x03/0x04，按协议文档重写4.7节 | Mr.Hu |
| V1.4 | 2026-08-14 | 新增6.4节「Qi 芯片 OTA (CAN-OTA 间接升级)」,明确 Host→CAN→MCU→UART→Qi 完整升级链路；更新全部文件路径至新项目目录；修复章节编号重复 | Mr.Hu |
| V1.5 | 2026-08-14 | AT32 取消自身 OTA，明确为 CAN-UART 通讯桥梁角色；删除双槽 Flash 布局（1.4简化为单应用区）、OTA 管理模块（6.1~6.3删除）、OTA 相关 DID（0x2112~0x2116删除）；软件架构中 OTA/启动管理替换为 CAN-UART 桥接管理 | Mr.Hu |
| V1.6 | 2026-08-15 | **MCU 恢复自身 OTA 能力**：基于 SRS 规范重新规划双槽 A/B 布局（1.4）；新增 6.1 MCU 自身 OTA 升级章节；6.2 改为 Qi 芯片实时中继模式；恢复 OTA 相关 DID（0x2112~0x2116）及新增 0x2117/0x2118；软件架构新增 Bootloader 层和 OTA 确认模块；UDS 0x34/0x36/0x37 安全等级改为 Level 1 并覆盖 MCU OTA；新增文件路径引用 | Mr.Hu |
| V1.7 | 2026-08-17 | **基于实际代码状态更新**：3.4 UDS 服务集标注实际实现状态（✅/⚠️/⬜）；6.1 MCU OTA 升级流程与 workflow-detail.md 对齐（删除未实现的 0x2E/0x31 步骤，补充实际代码流程时序图）；修复串口波特率（115200→9600，与代码一致）；新增待定事项 OI-06/011/012/013（Trial 触发链路/链接地址/active_slot 写回/ISO-TP）；参考文档新增 REF-09~16 交叉引用 | Mr.Hu |
