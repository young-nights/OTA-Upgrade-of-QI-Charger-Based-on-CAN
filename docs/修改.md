# 文档 vs 工程 / 脚本 — 修改清单（续）

> **复检日期**：2026-08-29（对照你已提交的文档修订：DID 对齐、32 字节、锁 30 s、功能寻址、APP `0x27`、Target IROM）
> **结论**：批次 A（文档口径）大部分已拍板并写入 3/5/6/7/8/9；D-3/D-4 已改。**代码和脚本仍全部未动**。文档还剩启动顺序、私钥路径、表格截断等。

---

## 进度总览

| 批次 | 内容 | 状态 |
| --- | --- | --- |
| A | 文档口径（DID、字符串长度、锁时、IROM、`0x27`） | **文档已定**；工程未跟 |
| B | Boot IROM 20 KB；APP SIT1145 + NC GPIO | **未做**（仅文档写了 Boot IROM 必须 20 KB） |
| C | Device Info + `0xF18C` + 产线脚本 | **文档有结构**；无 `device_info.c`、无打包脚本 |
| D | 脚本路径、HEX、擦除守卫 | **未做** |
| E | APP 充电业务 | **未做** |

---

## 1. 文档已拍板（不要再改需求，去改代码）

这些冲突已经在文档里统一，**实现按下面做**：

| 项 | 统一后的口径 | 代码仍是 |
| --- | --- | --- |
| 识别 DID | `0xF195` APP 版本、`0xF18C` SN、`0xF180` Boot 版本、`0xF193` 硬件版本 | APP：`F189/F18C="N/A"/F18D/F191`；Boot：`F189/F18D/F195` |
| DID 字符串 | 定长 **32 字节**，右补 ASCII 空格 | 16 字节 + `NUL` |
| 锁定时间 | **30 s** | `SECURITY_LOCKOUT_MS = 60000` |
| 功能寻址 | `0x18DB33xx`（PS=0x33，SA 任意） | 常量仍是 `0x18DB33D0`（掩码已能匹配，改名即可） |
| APP `0x27` | APP **要做** ECDSA，与 Boot 同一公钥 | APP 仍 NRC `0x11` |
| 区分 APP/Boot | **不要**再用 `7F 27 11` | 脚本仍靠 `0x27` NRC `0x11` |
| 链接 | 只配 Keil Target IROM，不用 scatter | APP 已对；**Boot `IROM` 仍是整片 `0x20000`** |
| XATO `version[16]` | 保持 16 字节，与 DID 无关 | 已对齐，不用动 |

---

## 2. 文档还要继续改（改漏 / 自相矛盾）

代码没写之前，先把这几处文档修干净，否则实现会跟错。

### D-1 文档 2 第六节启动顺序 — 仍是旧流程 【必改】

`2. Flash 分配方案.md` §六仍是：

```
[2] 检测启动原因
[3] 处理试运行
[4] 选槽
```

源码和文档 7 都是：**先判断 `ota_state==DOWNLOADING` → Safe Mode**，再 Trial、再选槽。漏了 DOWNLOADING 这一跳，产线/实验室会理解错。

同一文件 §五末尾还留着 `链接脚本配置: LR_IROM1 0x08000000 0x00005000`，与已改的 §八（Target IROM）重复且过时。删掉这行。

### D-2 文档 3 DID 表被说明截断 【必改】

`0xF18C` 后面插入了一段引用说明，Markdown 表格在 `0xF193 / 0xF195` 处断了。把说明挪到表外，表内四行 DID 连在一起。

### D-3 文档 7 互相打架 【已改 2026-08-29】

职责表与 §5.4：`0x2E 2010` 改为 Programming + `0x27` 解锁后可写；未解锁才 `0x33`。DID 表已拆开并修好截断；`0xF180` 改为编译期 Boot 版本。

### D-4 「用 `0xF180` + `0x2113` 区分 APP/Boot」不可用 【已改 2026-08-29】

文档 7 §3 / §6.1 / §10：探测改为 `0x34`（或 `0x31 01 FF00`）→ APP 回 NRC `0x11`。脚本 `zcanpro_ext_ota.py` 仍用 `0x27` NRC `0x11`，见 S-6。

### D-5 文档 8 私钥路径 — 【已改 2026-08-29】

正文与 OpenSSL 示例均改为 `docs/keys/private.pem`。

### D-6 文档 7 硬件表 / 产线脚本 【已改 2026-08-29】

文档 7 硬件表已补 SIT1145 / PA0 / PB1 / PB2 / NC。文档 9 改为相对路径 + `merge_prod_bin.py`。SN：**扫码枪 → CCU `0x2E F18C` → Device Info `0x0801D000`**，产线不用 J-Link 烧 SN。文档 3 `0xF18C` 访问改为读 + 产线写。代码仍缺 `0x2E F18C`（见 A-12）。

### D-7 文档 5 示例节点仍是 `0x0A` 【已改 2026-08-29】

物理寻址示例改为 CCU `0x03`、Qi `0x0D`：请求 `0x18DA0D03`，响应 `0x18DA030D`。

### D-8 霍尔路数：文档 1 vs SRS 【待书面确认，先别改 SRS 正文】

- 引脚表：仅 **PA0 一路** `H_OUT`（有磁场低、无磁场高）
- SRS：左右夹臂 **两路** 霍尔 → DID `0x2118`

不确认就不要在 APP 里做两路 GPIO。确认结果写进文档 1 或 SRS 备注即可。

### D-9 文档 4 IAP 帧 LEN 【组帧前核实】

示例 `LEN=0x1A` 与「LEN = CMD+DATA+流水号」差 1 字节。先抓 Qi 芯片真包，再写解析。不必先改文档定义。

---

## 3. 工程 / 脚本 — 全部仍待做

下面与上一版清单相同，**按新口径实现**（DID=`F195/F18C/F180/F193`，锁 30 s，APP 做 `0x27`）。

### 3.1 Bootloader

| ID | 优先级 | 问题 | 改哪里 |
| --- | --- | --- | --- |
| B-1 | P0 | Target IROM 整片 `0x20000` | `bootloader.uvprojx`：IROM1 = `0x08000000 / 0x5000` |
| B-2 | P0 | 无 Device Info | 新增 `device_info.h/.c`；`DEVICE_INFO_ADDR=0x0801D000` |
| B-3 | P0 | 擦除无地址守卫 | `erase_slot_flash()` 拒绝 `0x0801D000～0x0801DFFF` |
| B-4 | P1 | `0xF18C` 不读 Flash | Safe Mode `0x22` 读、`0x2E` 产线写 Device Info（与 APP A-12 同一布局） |
| B-5 | P1 | DID 仍是 F189/F18D | 改为 F180/F193/F195；旧 DID 可 NRC `0x31` 或只读别名（文档已不列旧号，建议直接换） |
| B-6 | P1 | 锁 60 s | `SECURITY_LOCKOUT_MS` → `30000` |
| B-7 | P1 | Seed=LFSR | 量产换批准熵源；现在可先加强混合 |
| B-8 | P2 | `boot_metadata.h` 仍写 scatter | 改成 Target IROM |
| B-9 | P2 | `trial_timer_callback` 未使用 | 删或标明 unused |

### 3.2 APP 板级 / DID / `0x27`

| ID | 优先级 | 问题 | 修改 |
| --- | --- | --- | --- |
| A-1 | P0 | 无 SIT1145 | 从 Boot 移植；`can_driver_init()` 先初始化收发器 |
| A-2 | P0 | NC 脚未拉低 | `board_gpio.c`：PA1、PB0、PB10、PA8/9/10/15、PB4/5/8 输出 0 |
| A-3 | P0 | PB2 5V 控制 | 默认低；使能充电拉高 |
| A-4 | P1 | PB1 12V Buck | 低=开；与储运模式一起定 |
| A-5 | P1 | PA0 霍尔 | 等 D-8 确认路数后再映射 `0x2118` |
| A-6 | P0 | `0xF18C` = `"N/A"` | 读 Device Info，32B 填充 |
| A-7 | P0 | 无 `DEVICE_INFO_*` | 与 Boot 同一头文件或拷贝结构 |
| A-8 | P1 | DID 编号 | `F195/F180/F193`，32 字节 |
| A-9 | P1 | `0x22` 单 DID | 多 DID；任一不支持整包 `0x31` |
| A-10 | P1 | `0x2E 2010` | 随 A-11，Programming + 解锁后允许写 `01` |
| A-11 | P1 | `0x27` NRC `0x11` | 实现与 Boot 相同的 ECDSA；**脚本探测同步改，不要分步合入** |
| A-12 | P0 | 产线 `0x2E F18C` | Programming + 解锁后把 32B SN 写入 Device Info（组 `DEVI`+CRC）；默认会话拒绝写；`0x22 F18C` 读回（文档 9 §6.2） |

### 3.3 APP 充电（文档 4 + SRS）

| ID | 优先级 | 内容 |
| --- | --- | --- |
| Q-1 | P0 | UART 帧 `55 AA` + CS（先核实 LEN） |
| Q-2 | P0 | 解析 `0x01` 状态 → `0x2102/0x2109` |
| Q-3 | P0 | 下发 `0x02` 功率 ↔ `0x210D` |
| Q-4 | P1 | Qi IAP `0xCC`（可后置） |
| Q-5 | P0 | 充电状态机；Programming 200 ms 内关功率 |
| Q-6 | P0 | DID `0x2100～0x2111`、`0x2117`、`0x2118` |
| Q-7 | P0 | 生命周期 byte1～7 填充电快照 |
| Q-8 | P1 | 例程 `0x31 01 2100/2101/2102` |
| Q-9 | P1 | NVM 持久化 `210D/210E/210F/2117` |
| Q-10 | P1 | 储运模式 `210F=0x03` |
| Q-11 | P2 | SRS 要看门狗 vs 文档 7「APP 无 IWDG」——仍需书面确认 |

OTA：`2112～2116`、`10 02`+`11` 进 Boot、无 IWDG Trial — **保持，不要回退**。

### 3.4 脚本

| ID | 优先级 | 文件 | 仍要做 |
| --- | --- | --- | --- |
| S-1 | P0 | `pack_image.py` | 用 `dirname` 推仓库根，去掉写死的 `I:\...` |
| S-2 | P0 | `zcanpro_ext_ota.py` | 同上 |
| S-3 | P0 | `merge_prod_bin.py --hex` | 每 64 KB 写 ELA；Boot+满槽 A 会跨 `0x08010000` |
| S-4 | P1 | `merge_prod_bin.py` | APP ≤ `0xB800`。SN 改走 UDS，**不必**再把 Device Info 嵌进合并 bin |
| S-5 | — | `pack_device_info.py` | **取消产线主路径**（SN 不经文件烧录）。仅调试/返工备用，可不做 |
| S-6 | P1 | `zcanpro_ext_ota.py` | 探测改为 `0x34` NRC `0x11`（见 D-4），不要再用 `0x27` |
| S-7 | P2 | 使用说明 | 补合并 bin、SN、探测方式 |

---

## 4. 建议下一轮改什么（文档优先，小而确定）

只动文档、一次能合完：

1. 文档 2：启动顺序 + 删「链接脚本」残留  
2. 文档 3 / 7：修好被截断的 DID 表  
3. ~~文档 7：删「永不解锁」；改 APP/Boot 探测~~（D-3 / D-4 已改）  
4. ~~文档 8：私钥改 `docs/keys/private.pem`~~（D-5 已改）  
5. ~~文档 5 / 7 硬件表 / 文档 9 产线与 SN~~（D-6、D-7 已改）

代码第一刀仍建议 **B-1（Boot IROM 20 KB）**，与文档 2/9 已经一致，再不改有变砖风险。

---

## 5. 已对齐、不要重做

- Flash A/B 46 KB、头 256 B、入口 `0x08005100` / `0x08010900`
- APP Target IROM、无 scatter（工程已对）
- 双槽 ping-pong、`0x31 FF00` 再 `0x34`、ISO-TP、`0x37` 置 PENDING
- APP 触发 `0x10 0x02` + `0x11`，下载 SID 回 `0x11`
- CAN 250 kbps、采样点 87.5%
- 生命周期 `0x18FF260D`、BOOTUP + 1 Hz
- `pack_image.py` 头格式与 `boot_verify.h` 一致（只是默认路径写死）

`docs/backup/` 是旧稿，不必同步。`docs/keys/` 不要提交到公开远程。
