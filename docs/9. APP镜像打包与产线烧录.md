# 9. APP 镜像打包与产线烧录

> **适用**：Keil 编出 Slot A 裸 `.bin` 后加 XATO 头，合并 Boot，产线烧录；SN 由扫码 + CCU UDS 写入
> **脚本**（路径均相对**仓库根**）：
> - `python_tools/pack_image.py` — 裸 bin → `app_slot_a.ota.bin`
> - `python_tools/merge_prod_bin.py` — Boot + Slot A → 一份 `prod_image.bin`
> **不需要**：ZCANPRO、`python-can`、`cryptography`（打包/合并用标准库）

下文路径都相对仓库根，不要写死盘符。在仓库根下执行示例命令。

产线正式烧录只烧 **Bootloader + Slot A**（可用合并后的单文件）。Slot B 出厂空着，留给以后 CAN OTA。SN **不**打进这份镜像，上电进 APP 后由 CCU 写入。

---

## 1. Flash 布局（和打包的关系）

```
0x08000000  Bootloader  28KB
0x08007000  Slot A 头 256B     ← pack_image.py 加上的 XATO 头
0x08007100  Slot A 代码        ← Keil Target → IROM1 从这里开始
0x08011800  Slot B 头 256B     ← 出厂不烧
0x08011900  Slot B 代码
0x0801D000  Device Info 4KB    ← SN 序列号等设备信息，OTA 擦写跳过
```

- Keil 链接地址是 **代码入口** `0x08007100`，产出的裸 `.bin` **不含** 头。
- 打包后的 `.ota.bin` = **256 字节头 + 裸 bin**，必须从槽起始 **`0x08007000`** 烧录。
- 不要把裸 `.bin` 直接烧到 `0x08007100`：没有头则 Bootloader 验签失败，一直停在 Safe Mode。

---

## 2. Keil 生成 Slot A 裸 `.bin`

工程：`qi_wireless_code_slotA/mdk_project/qi_wireless.uvprojx`（Slot A 工程）
Slot B 工程：`qi_wireless_code_slotB/mdk_project/qi_wireless.uvprojx`

**只在 Keil Target 里配 IROM/IRAM，不用 scatter 文件。**

Options for Target（魔术棒）→ **Target**：

| 项               | Slot A（产线 / 默认工程）     | Slot B（从 APP 做 CAN 升级时另编） |
| ---------------- | ----------------------------- | ---------------------------------- |
| IROM1 Start      | `0x08007100`                | `0x08011900`                     |
| IROM1 Size       | `0xA700`（42KB − 256B 头） | `0xA700`                         |
| IRAM1 Start      | `0x20000000`                | `0x20000000`                     |
| IRAM1 Size       | `0x5000`                    | `0x5000`                         |
| default in IROM1 | 勾选（代码放进 IROM1）        | 勾选                               |

Options for Target → **Linker**：

- **勾选** “Use Memory Layout from Target Dialog”（用上面 Target 的 IROM/IRAM 生成链接）
- **不要勾选** Scatter File，也不要再使用已删除的 `scatter\app_slot_*.sct`

当前仓库工程即为 Slot A：IROM `0x08007100` / `0xA700`，`umfTarg=1`。编 Slot B 时只改 Target 里 IROM1 Start 为 `0x08011900`，编完再改回，避免下次误链到 B。

Keil 默认只出 `.axf`。在 **Options for Target → User → After Build/Rebuild** 勾选第一项，命令填：

```text
fromelf --bin --output="$L@L.bin" "#L"
```

Rebuild 后得到：

```text
qi_wireless_code_slotA/mdk_project/Objects/qi_wireless.bin
```

文件大小必须 **≤ 42752 字节**（`0xA700`）。加上头后总长 ≤ 槽大小 `0xA800`（42KB）。

可用 HxD 看裸 `.bin` 开头 8 字节（小端）：

| 偏移 | 含义          | Slot A 大致样子                                        |
| ---- | ------------- | ------------------------------------------------------ |
| 0..3 | 初始 MSP      | `00 xx 00 20`（SRAM `0x2000xxxx`）                 |
| 4..7 | Reset Handler | 落在`0x080071xx` 一带（最低位为 1 表示 Thumb，正常） |

若 Reset 还在 `0x08000000` 附近，说明 IROM 仍按整片 Flash 链接，不能打包。

---

## 3. 准备私钥

私钥必须与烧进 Bootloader 的 ECDSA P-256 公钥成对。量产请换正式密钥。

放到：

```text
docs/keys/private.pem
```

仓库默认不附带密钥文件。必须与烧进 Bootloader 的公钥成对。

---

## 4. 用 `pack_image.py` 加头

脚本：`python_tools/pack_image.py`。依赖同目录签名函数，**不需要打开 ZCANPRO**。

在仓库根目录：

```text
cd python_tools
python pack_image.py
```

默认输入/输出（相对仓库根）：

| 参数          | 相对仓库根的路径                                          |
| ------------- | --------------------------------------------------------- |
| `--bin`     | `qi_wireless_code_slotA/mdk_project/Objects/qi_wireless.bin`    |
| `--key`     | `docs/keys/private.pem`                                   |
| `--out`     | `python_tools/app bin/app_slot_a.ota.bin`（自动输出到 `app bin/` 目录） |
| `--version` | `1.0.0`（写入镜像头，DID `0xF195` 读这个字符串）        |

```text
python pack_image.py --bin ../qi_wireless_code_slotA/mdk_project/Objects/qi_wireless.bin --key ../docs/keys/private.pem --out "app bin/app_slot_a.ota.bin" --version 1.0.0
```

> 脚本会自动按槽命名输出文件到 `python_tools/app bin/` 目录。

成功时打印类似：

```text
固件无头，现场打包 xxxx 字节
打包完成 crc=0x........ version=1.0.0
镜像链接 Slot A, 总长 yyyy
输出: ...\app_slot_a.ota.bin
总长: yyyy  (头 256 + 固件 xxxx)
链接: Slot A  → 产线烧录地址 0x08007000
```

若 **链接不是 Slot A**，说明 Target 的 IROM1 不是 `0x08007100`，不要拿去烧。

常见失败：

| 提示                        | 原因                                                   | 处理                              |
| --------------------------- | ------------------------------------------------------ | --------------------------------- |
| 找不到固件                  | 还没`fromelf` 出 `.bin`，或路径不对                | 按第 2 节生成后再跑               |
| 找不到私钥                  | `docs/keys/private.pem` 不存在                       | 放入与 Bootloader 成对的 PEM      |
| 超过槽大小                  | 裸 bin 大于`0xA700`                                  | 减小 APP 或核对 Target IROM1 Size |
| Reset Handler 不在 Slot A/B | Target IROM1 Start 不是`0x08007100` / `0x08011900` | 改 Target IROM 后 Rebuild         |

---

## 5. 头里有什么

`image_header_t` 共 256 字节，与 Bootloader `boot_verify.h` 一致：

| 偏移 | 长度 | 字段            | 说明                           |
| ---- | ---- | --------------- | ------------------------------ |
| 0x00 | 4    | magic           | `0x4F544158`（"XATO"，小端） |
| 0x04 | 4    | image_length    | 裸固件字节数（不含头）         |
| 0x08 | 4    | crc32           | 裸固件 CRC32（IEEE 802.3）     |
| 0x0C | 64   | signature       | ECDSA P-256，`R\|\|S`          |
| 0x4C | 16   | version         | `--version`，如 `1.0.0\0`  |
| 0x5C | 4    | build_timestamp | 打包时的 Unix 时间             |
| 0x60 | 160  | reserved        | 填 0                           |

签名对象是 **裸固件**（不含头），算法与 Bootloader `uECC` / SHA-256 一致。

---

## 6. 产线怎么烧

推荐：先合并成一个 bin，从 `0x08000000` 烧一次。SN **不要**打进这份镜像。

| 区域 | 地址 | 产线动作 |
| --- | --- | --- |
| Bootloader | `0x08000000` | 合并镜像前 28 KB |
| Slot A | `0x08007000` | 合并镜像中的 `app_slot_a.ota.bin`（含头） |
| Device Info | `0x0801D000` | 出厂为擦除态 `0xFF`；**上电后**由 CCU 经 UDS 写入 SN |
| Slot B | `0x08011800` | **不烧** |

上电后 Bootloader 校验 Slot A 通过即跳 APP。Slot B 空着是正常的。Device Info 空着时 `0x22 F18C` 回 NRC `0x31`（尚未赋码）。

### 6.1 合并 Boot + Slot A（`merge_prod_bin.py`）

脚本：`python_tools/merge_prod_bin.py`（用 `dirname` 推仓库根，不写死盘符）。

```text
cd python_tools
python merge_prod_bin.py
```

默认：

| 参数 | 相对仓库根 |
| --- | --- |
| `--boot` | `qi_wireless_bootloader/mdk_project/Objects/bootloader.bin` |
| `--app` | `python_tools/app bin/app_slot_a.ota.bin` |
| `--out` | `python_tools/prod_image.bin` |

布局：偏移 `0x0000` = Boot（不足 28 KB 补 `0xFF`），偏移 `0x7000` = Slot A 打包文件。用 J-Link / AT32 ISP 从 **`0x08000000`** 下载 `prod_image.bin`。

`--hex` 可选。Boot 28 KB + 满槽 A 会跨 `0x08010000`，HEX 必须按 64 KB 换 Extended Linear Address（脚本若未分段，产线只用 `.bin`）。

APP 总长必须 ≤ `0xA800`。合并文件**不含** SN。

### 6.2 产线写入 SN（扫码 → CCU → CAN-UDS）

**不经过 J-Link。** 工位流程：

```
扫码枪读产品条码
    → CCU 得到 SN 字符串
    → CAN UDS 写到本模块 Flash Device Info（0x0801D000）
    → 读回校验
```

前提：第 6.1 节镜像已烧、模块已上电并跑在 **APP**（`0x34` 回 `7F 34 11`）。CAN：250 kbps，请求 `0x18DA0D03`，响应 `0x18DA030D`。

| 步 | 谁 | UDS | 说明 |
| --- | --- | --- | --- |
| 1 | 操作员 | — | 扫码枪扫产品条码，CCU 得到 SN（ASCII，≤ 31 字符） |
| 2 | CCU | `10 02` | Programming Session |
| 3 | CCU | `27 01` / `27 02` | SecurityAccess Level 1（ECDSA，私钥在 CCU/产线侧） |
| 4 | CCU | `2E F18C` + SN | 定长 32 字节，不足右补空格 `0x20`，过长截断并失败（NRC `0x31`） |
| 5 | 模块 | — | APP 组 `device_info_t`（magic `DEVI`、version=1、sn、可选 hw_version/日期、CRC32）写入 `0x0801D000` |
| 6 | CCU | `22 F18C` | 读回应与扫码一致（32 字节空格填充） |

正响应：`6E F1 8C`。未进 Programming 或未解锁：NRC `0x22` / `0x33`。

Device Info 布局（v2，512 字节有效，详见文档 2）：

| 偏移 | 长度 | 字段 | 说明 |
|------|------|------|------|
| 0x00 | 4 | magic | `0x44455649` ("DEVI") |
| 0x04 | 4 | version | 格式版本 = 2（v2 新增公钥字段） |
| 0x08 | 4 | crc32 | 覆盖其余字段（与 `boot_crc32` 相同算法） |
| 0x0C | 32 | sn | ASCII，与 DID 载荷对应 |
| 0x2C | 8 | hw_version | 可选；不写则保持 `0xFF` 或编译期默认 |
| 0x34 | 4 | production_date | 可选，Unix 时间戳 |
| 0x38 | 65 | ecdsa_pubkey | SEC1 未压缩 ECDSA P-256 公钥，支持密钥轮换 |
| 0x79 | 1 | pubkey_valid | `0x01`=有效，`0xFF`=未配置 |
| 0x7A | 390 | reserved | `0xFF` |

约束：

- 默认会话 **只读** `0xF18C`，防止售后误改。
- 是否允许产线二次写入（返工改码）由 CCU/工装策略决定；模块侧建议仅在 SN 仍为全 `0xFF` 或显式返工会话下接受覆盖。
- OTA `0x31 FF00` 只擦 A/B 槽，**禁止**擦 `0x0801D000～0x0801DFFF`，升级后 SN 仍在。
- 空片/未赋码：`0x22 F18C` → NRC `0x31`。

Safe Mode 下同样实现 `0x2E F18C`，便于「只烧了 Boot、还没跳 APP」的工位；正式线优先在 APP 里写。

---

## 7. 和 CAN OTA 的关系

| 场景                                | 镜像                          | 说明                                                                                                                                               |
| ----------------------------------- | ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 产线 / 空片双槽无效                 | 本页打出的 Slot A`.ota.bin` | 烧到`0x08007000`，或交给 ZCANPRO 下发                                                                                                            |
| APP 已在 Slot A 跑，第一次 CAN 升级 | **必须另编 Slot B**     | Slot B 工程：`qi_wireless_code_slotB/`，Target IROM1 Start 改为`0x08011900`（Size 仍 `0xA700`），Rebuild 后再对那份裸 bin 跑 `pack_image.py`（或让对应槽脚本现场打包） |

不能把 Slot A 的包拿去升 Slot B：MCU 写入非活跃槽，Reset Handler 必须落在该槽内，否则 `0x37` 失败。

ZCANPRO 扩展脚本已按槽拆分：

- `python_tools/zcanpro_ext_ota_slotA.py` — Slot A 专用 OTA 脚本
- `python_tools/zcanpro_ext_ota_slotB.py` — Slot B 专用 OTA 脚本
- `python_tools/zcanpro_ext_ota_auto.py` — 自动选择槽 OTA 脚本

各脚本中：

- `FIRMWARE_PATH` 可填裸 `.bin`（运行时自动加头）或已打好的 `.ota.bin`（检测到 XATO 则原样下发）。
- 空片进 Safe Mode：`RESET_APP_TO_BOOT = False`，用 Slot A 包。
- 从 APP 触发升级：`RESET_APP_TO_BOOT = True`，用 Slot B 包。

详细 UDS 步骤见 `python_tools/脚本使用说明.md`。

---

## 8. 步骤清单（Slot A 产线包）

1. Keil Target：IROM1 `0x08007100` / `0xA700`，勾选 Use Memory Layout from Target Dialog；After Build `fromelf` 出 `qi_wireless.bin`。
2. 确认 `docs/keys/private.pem` 存在且与 Bootloader 公钥成对。
3. 仓库根下 `cd python_tools` 后 `python pack_image.py`。日志必须是 **链接 Slot A**。
4. `cd python_tools` 后 `python merge_prod_bin.py`，得到 `prod_image.bin`。
5. 将该文件从 **`0x08000000`** 烧进芯片（ISP / J-Link）。不要单独再烧 SN 二进制。
6. 上电应进入 APP（`0x34` → `7F 34 11`），不要停在 Safe Mode。
7. 扫码枪扫产品条码 → CCU `10 02` + `0x27` 解锁 + `2E F18C` 写 SN → `22 F18C` 核对。

---

## 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | 2026-08-28 | 初始版本 |
| v1.1 | 2026-08-29 | 新增 Device Info 保留区（SN 存储）：Flash 布局图新增 Device Info 区域，产线烧录流程新增 SN 烧录步骤（含 J-Link 命令行示例），明确 OTA 擦写跳过 Device Info 区域 |
| v1.2 | 2026-08-29 | 路径改为相对仓库根；补充 `merge_prod_bin.py`；SN 改为扫码枪 → CCU `0x2E F18C` 写 Device Info，取消产线 J-Link 烧 SN |
| v1.3 | 2026-09-04 | 工程目录拆分：`qi_wireless_code/` → `qi_wireless_code_slotA/` + `qi_wireless_code_slotB/`；OTA 脚本拆分：`zcanpro_ext_ota.py` → `zcanpro_ext_ota_slotA.py` / `zcanpro_ext_ota_slotB.py` / `zcanpro_ext_ota_auto.py`；`pack_image.py` 输出到 `app bin/` 目录；Device Info 布局升级至 v2（新增 ecdsa_pubkey + pubkey_valid） |
