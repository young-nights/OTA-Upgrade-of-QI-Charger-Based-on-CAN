# 9. APP 镜像打包与产线烧录

> **适用**：Keil 编出 Slot A 裸 `.bin` 后，用脚本加 256 字节 XATO 头
> **脚本**：`qi_wireless_bootloader/tools/pack_image.py`
> **Windows 仓库根目录**：`I:\GitHub-young-nights\ota-upgrade-of-qi-charger-based-on-can`
> **不需要**：ZCANPRO、`python-can`、`cryptography`

产线正式烧录只烧 **Bootloader + Slot A**。Slot B 出厂空着，留给以后 CAN OTA。

---

## 1. Flash 布局（和打包的关系）

```
0x08000000  Bootloader  20KB
0x08005000  Slot A 头 256B     ← pack_image.py 加上的 XATO 头
0x08005100  Slot A 代码        ← Keil Target → IROM1 从这里开始
0x08010800  Slot B 头 256B     ← 出厂不烧
0x08010900  Slot B 代码
```

- Keil 链接地址是 **代码入口** `0x08005100`，产出的裸 `.bin` **不含** 头。
- 打包后的 `.ota.bin` = **256 字节头 + 裸 bin**，必须从槽起始 **`0x08005000`** 烧录。
- 不要把裸 `.bin` 直接烧到 `0x08005100`：没有头则 Bootloader 验签失败，一直停在 Safe Mode。

---

## 2. Keil 生成 Slot A 裸 `.bin`

工程：`qi_wireless_code\mdk_project\qi_wireless.uvprojx`

**只在 Keil Target 里配 IROM/IRAM，不用 scatter 文件。**

Options for Target（魔术棒）→ **Target**：

| 项               | Slot A（产线 / 默认工程）     | Slot B（从 APP 做 CAN 升级时另编） |
| ---------------- | ----------------------------- | ---------------------------------- |
| IROM1 Start      | `0x08005100`                | `0x08010900`                     |
| IROM1 Size       | `0xB700`（46KB − 256B 头） | `0xB700`                         |
| IRAM1 Start      | `0x20000000`                | `0x20000000`                     |
| IRAM1 Size       | `0x5000`                    | `0x5000`                         |
| default in IROM1 | 勾选（代码放进 IROM1）        | 勾选                               |

Options for Target → **Linker**：

- **勾选** “Use Memory Layout from Target Dialog”（用上面 Target 的 IROM/IRAM 生成链接）
- **不要勾选** Scatter File，也不要再使用已删除的 `scatter\app_slot_*.sct`

当前仓库工程即为 Slot A：IROM `0x08005100` / `0xB700`，`umfTarg=1`。编 Slot B 时只改 Target 里 IROM1 Start 为 `0x08010900`，编完再改回，避免下次误链到 B。

Keil 默认只出 `.axf`。在 **Options for Target → User → After Build/Rebuild** 勾选第一项，命令填：

```text
fromelf --bin --output="$L@L.bin" "#L"
```

Rebuild 后得到：

```text
I:\GitHub-young-nights\ota-upgrade-of-qi-charger-based-on-can\qi_wireless_code\mdk_project\Objects\qi_wireless.bin
```

文件大小必须 **≤ 46848 字节**（`0xB700`）。加上头后总长 ≤ 槽大小 `0xB800`（46KB）。

可用 HxD 看裸 `.bin` 开头 8 字节（小端）：

| 偏移 | 含义          | Slot A 大致样子                                        |
| ---- | ------------- | ------------------------------------------------------ |
| 0..3 | 初始 MSP      | `00 xx 00 20`（SRAM `0x2000xxxx`）                 |
| 4..7 | Reset Handler | 落在`0x080051xx` 一带（最低位为 1 表示 Thumb，正常） |

若 Reset 还在 `0x08000000` 附近，说明 IROM 仍按整片 Flash 链接，不能打包。

---

## 3. 准备私钥

私钥必须与烧进 Bootloader 的 ECDSA P-256 公钥成对。量产请换正式密钥。

放到：

```text
I:\GitHub-young-nights\ota-upgrade-of-qi-charger-based-on-can\docs\keys\private.pem
```

仓库默认不附带密钥文件。

---

## 4. 用 `pack_image.py` 加头

脚本路径：

```text
I:\GitHub-young-nights\ota-upgrade-of-qi-charger-based-on-can\qi_wireless_bootloader\tools\pack_image.py
```

依赖同目录的 `zcanpro_ext_ota.py`（只用其中的签名/打包函数，**不需要打开 ZCANPRO**）。

在 **cmd** 或 PowerShell：

```Python
cd d 
cd I:\GitHub-young-nights\ota-upgrade-of-qi-charger-based-on-can\qi_wireless_bootloader\tools
python pack_image.py
```

默认输入/输出（仓库在上述路径时可直接回车跑）：

| 参数          | 默认路径                                                        |
| ------------- | --------------------------------------------------------------- |
| `--bin`     | `...\qi_wireless_code\mdk_project\Objects\qi_wireless.bin`    |
| `--key`     | `...\docs\keys\private.pem`                                   |
| `--out`     | `...\qi_wireless_code\mdk_project\Objects\app_slot_a.ota.bin` |
| `--version` | `1.0.0`（写入镜像头，APP 的 DID `0xF189` 会读这个字符串）   |

显式指定路径和版本：

```text
python pack_image.py --bin I:\GitHub-young-nights\ota-upgrade-of-qi-charger-based-on-can\qi_wireless_code\mdk_project\Objects\qi_wireless.bin --key I:\GitHub-young-nights\ota-upgrade-of-qi-charger-based-on-can\docs\keys\private.pem --out I:\GitHub-young-nights\ota-upgrade-of-qi-charger-based-on-can\qi_wireless_code\mdk_project\Objects\app_slot_a.ota.bin --version 1.0.0
```

成功时打印类似：

```text
固件无头，现场打包 xxxx 字节
打包完成 crc=0x........ version=1.0.0
镜像链接 Slot A, 总长 yyyy
输出: ...\app_slot_a.ota.bin
总长: yyyy  (头 256 + 固件 xxxx)
链接: Slot A  → 产线烧录地址 0x08005000
```

若 **链接不是 Slot A**，说明 Target 的 IROM1 不是 `0x08005100`，不要拿去烧。

常见失败：

| 提示                        | 原因                                                   | 处理                              |
| --------------------------- | ------------------------------------------------------ | --------------------------------- |
| 找不到固件                  | 还没`fromelf` 出 `.bin`，或路径不对                | 按第 2 节生成后再跑               |
| 找不到私钥                  | `docs\keys\private.pem` 不存在                       | 放入与 Bootloader 成对的 PEM      |
| 超过槽大小                  | 裸 bin 大于`0xB700`                                  | 减小 APP 或核对 Target IROM1 Size |
| Reset Handler 不在 Slot A/B | Target IROM1 Start 不是`0x08005100` / `0x08010900` | 改 Target IROM 后 Rebuild         |

---

## 5. 头里有什么

`image_header_t` 共 256 字节，与 Bootloader `boot_verify.h` 一致：

| 偏移 | 长度 | 字段            | 说明                           |
| ---- | ---- | --------------- | ------------------------------ |
| 0x00 | 4    | magic           | `0x4F544158`（"XATO"，小端） |
| 0x04 | 4    | image_length    | 裸固件字节数（不含头）         |
| 0x08 | 4    | crc32           | 裸固件 CRC32（IEEE 802.3）     |
| 0x0C | 64   | signature       | ECDSA P-256，`R\|\|S`          |
| 0x4C | 16   | version         | `--version`，如 `1.0.0\0`  |
| 0x5C | 4    | build_timestamp | 打包时的 Unix 时间             |
| 0x60 | 160  | reserved        | 填 0                           |

签名对象是 **裸固件**（不含头），算法与 Bootloader `uECC` / SHA-256 一致。

---

## 6. 产线怎么烧

| 区域       | 地址                     | 文件                                     |
| ---------- | ------------------------ | ---------------------------------------- |
| Bootloader | `0x08000000`           | Bootloader 的`.bin` / `.axf`         |
| Slot A     | **`0x08005000`** | **`app_slot_a.ota.bin`（含头）** |
| Slot B     | `0x08010800`           | **不烧**                           |

上电后 Bootloader 校验 Slot A 通过即跳 APP。Slot B 空着是正常的。

---

## 7. 和 CAN OTA 的关系

| 场景                                | 镜像                          | 说明                                                                                                                                               |
| ----------------------------------- | ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 产线 / 空片双槽无效                 | 本页打出的 Slot A`.ota.bin` | 烧到`0x08005000`，或交给 ZCANPRO 下发                                                                                                            |
| APP 已在 Slot A 跑，第一次 CAN 升级 | **必须另编 Slot B**     | Target IROM1 Start 改为`0x08010900`（Size 仍 `0xB700`），Rebuild 后再对那份裸 bin 跑 `pack_image.py`（或让 `zcanpro_ext_ota.py` 现场打包） |

不能把 Slot A 的包拿去升 Slot B：MCU 写入非活跃槽，Reset Handler 必须落在该槽内，否则 `0x37` 失败。

ZCANPRO 扩展脚本 `zcanpro_ext_ota.py`：

- `FIRMWARE_PATH` 可填裸 `.bin`（运行时自动加头）或已打好的 `.ota.bin`（检测到 XATO 则原样下发）。
- 空片进 Safe Mode：`RESET_APP_TO_BOOT = False`，用 Slot A 包。
- 从 APP 触发升级：`RESET_APP_TO_BOOT = True`，用 Slot B 包。

详细 UDS 步骤见 `qi_wireless_bootloader/tools/zcanpro_ext_ota使用说明.md`。

---

## 8. 步骤清单（Slot A 产线包）

1. Keil Target：IROM1 `0x08005100` / `0xB700`，勾选 Use Memory Layout from Target Dialog；After Build `fromelf` 出 `qi_wireless.bin`。
2. 确认 `docs\keys\private.pem` 存在且与 Bootloader 公钥成对。
3. `cd ...\qi_wireless_bootloader\tools` 后执行 `python pack_image.py`。
4. 确认日志为 **链接 Slot A**、烧录地址 **`0x08005000`**。
5. Bootloader → `0x08000000`；`app_slot_a.ota.bin` → `0x08005000`。
6. 上电应进入 APP，不要进 Safe Mode。
